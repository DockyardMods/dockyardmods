# Dev Fixtures

Minimal local Dockyard ecosystem data for development and mock UI work.

## Layout

- `registry.json` - registry fixture with mock mod entries
- `manifests/<mod-id>/manifest.yaml` - manifest fixture for each registry entry

Use `fixtures/dev` as the root with `createDockyardEcosystemLayout`.
